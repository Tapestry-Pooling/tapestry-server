import Login from 'containers/Login';
import UploadTest from 'containers/UploadTest';

const routes = [
  {
    path: '/app/login',
    component: Login,
  },
  {
    path: '/app/upload-test',
    component: UploadTest,
  },
];

export default routes;
